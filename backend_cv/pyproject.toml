[project]
name = "backend-cv"
version = "0.1.0"
description = "Backend CV - Generador de CV con IA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "groq",
    "python-dotenv",
    "fpdf2==2.8.5",
    "requests",
    "httpx",
    "cachetools==7.0.1",
]
